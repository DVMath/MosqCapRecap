
model {

for (t in 1:totT) {
X[t] <- pow(phi,t)*p
Yu[t] ~ dbin(p, U)
}

theta <- sum(X[])
Ncapt ~ dpois(theta* N)

Y ~ dmulti(X, Ncapt)
#U ~ dnegbin(l, v)
U ~ dpois(v)

#l ~ dbeta(2, 2)
v ~ dgamma(0.01, 0.01)

#p ~ dbeta(2,2)
phi ~ dbeta(4,2)
p ~ dbeta(2,4)
}